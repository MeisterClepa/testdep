const DEP_NAME = "es6-examples";
const DEP_VERSION = "0.0.1";
const FEATURE_FLAGS = Object.freeze({
  enableFoo: true,
  enableBar: false,
});

export default {
  DEP_NAME,
  DEP_VERSION,
  FEATURE_FLAGS,
};
